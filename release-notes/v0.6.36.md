# Xtension v0.6.36

- Keeps OpenAI Codex as the first writing engine and uses Claude only as the configured fallback.
- Isolates the connector's Codex session from unrelated user MCP tools so repeated generations stay fast.
- Releases completed Codex threads and terminates the full connector process tree on restart, preventing accumulated helper processes from causing 20-second startup timeouts.
- Retains the delayed X editor recovery from the previous fix so generated corrections can still be inserted after an editor refresh.
