# Xtension v0.4.8

- Clarifies the README so end users are not told to install Node.js, Python, or Pillow.
- Makes `npm run build` require Node.js only by using committed runtime and store assets.
- Keeps Python and Pillow only for the optional `npm run build:assets` maintenance step.

Archives:

- `xtension-edge-v0.4.8.zip`
- `xtension-chrome-v0.4.8.zip`
- `xtension-firefox-v0.4.8.zip`
